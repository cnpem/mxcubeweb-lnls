%YAML 1.2
---
class: LNLS.EPICS.EPICSActuator.LNLSEnergy
epics:
  "":
    channels:
      rbv:
        suffix: "SWC:MOTORS:m1.VAL"
        polling_period: 200
      val:
        suffix: "SWC:MOTORS:m1.VAL"
configuration:
  tolerance: 0.01
  plan_name: "move_energy_and_phase"
  plan_parameter: "energy"
  default_limits: (5, 20)
