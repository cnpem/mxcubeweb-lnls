%YAML 1.2
---
class: LNLS.LNLSTransmission.LNLSTransmission
epics:
  "MNC:B:TRANSMISSION:":
    channels:
      rbv:
        suffix: "RBV"
        polling_period: 200
      val:
        suffix: "SET"
configuration:
  tolerance: 0.5
  default_limits: (0, 100)
  default_timeout: 10
